document.querySelectorAll('nav button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active')}));document.getElementById('new').addEventListener('click',()=>alert('Intervenție nouă — conectarea la baza de date urmează în etapa următoare.'));