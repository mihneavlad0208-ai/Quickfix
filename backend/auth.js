const jwt=require("jsonwebtoken");function signUser(user){return jwt.sign({sub:user.id,email:user.email,role:user.role},{})}function requireAuth(req,res,next){const h=req.headers.authorization||"";const token=h.startsWith("Bearer ")?h.slice(7):null;if(!token||!process.env.JWT_SECRET)return res.status(401).json({error:"Autentificare necesară"});try{req.user=jwt.verify(token,process.env.JWT_SECRET);next()}catch{return res.status(401).json({error:"Token invalid sau expirat"})}}module.exports={signUser,requireAuth};